<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wguapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ui.activities.AssessmentDetailActivity"></activity>
        <activity android:name=".ui.activities.MentorDetailActivity" />
        <activity android:name=".ui.activities.NoteDetailActivity" />
        <activity android:name=".ui.activities.CourseDetailActivity" />
        <activity android:name=".ui.activities.TermDetailActivity" />
        <activity android:name=".ui.activities.TermListActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".ui.activities.MainActivity"/>
        <receiver android:name=".util.NotificationPublisher"  android:exported="true" android:enabled="true"/>

    </application>

</manifest>